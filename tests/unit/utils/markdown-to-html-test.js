import markdownToHtml from 'coder-note/utils/markdown-to-html';
import { module, test } from 'qunit';

module('Unit | Utility | markdown-to-html', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = markdownToHtml();
    assert.ok(result);
  });
});
